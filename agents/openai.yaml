version: 1
display_name: OpenClaw Vertex Setup
short_description: Configure OpenClaw to use Vertex AI Gemini models end to end.
default_prompt: Configure or repair OpenClaw so it uses Vertex AI Gemini models through normal startup paths, including shared env setup, Gateway service startup, local agent verification, and TUI verification.
